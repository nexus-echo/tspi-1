# --- tspi_ai_brain runtime deps ---
# Minimum-version pins (>=) so pip selects wheels compatible with your Python.
# Hard "==" pins can fail on newer Python versions when wheels aren't published.

fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
httpx>=0.28

# Axis dependency graph + root-cause ranking
networkx>=3.4
scipy>=1.18

# --- Database ---
SQLAlchemy>=2.0.36
alembic>=1.13

# PostgreSQL drivers
#
# psycopg v3 — modern PostgreSQL driver
psycopg[binary]>=3.2

# psycopg2 compatibility driver.
# Required because the current SQLAlchemy configuration/runtime
# is loading sqlalchemy.dialects.postgresql.psycopg2.
psycopg2-binary>=2.9.9

# PostgreSQL pgvector support
pgvector>=0.3.6


# --- dev / test ---
pytest>=8.3
pytest-asyncio>=0.25


# --- lab/imaging extraction (POST /extract) ---
python-multipart>=0.0.9

# PDF text-layer extraction
pdfplumber>=0.11

# Rasterize scanned PDF pages -> images for the vision model
PyMuPDF>=1.24

cryptography>=42
